steps:
  - name: gcr.io/cloud-builders/docker
    entrypoint: bash
    args:
      - "-c"
      - |
        docker pull $_AR_HOSTNAME/$PROJECT_ID/cloud-run-source-deploy/$REPO_NAME/$_SERVICE_NAME:latest || exit 0
    id: Pull docker cache
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - "-t"
      - $_AR_HOSTNAME/$PROJECT_ID/cloud-run-source-deploy/$REPO_NAME/$_SERVICE_NAME:$COMMIT_SHA
      - "-t"
      - $_AR_HOSTNAME/$PROJECT_ID/cloud-run-source-deploy/$REPO_NAME/$_SERVICE_NAME:latest
      - .
      - --cache-from
      - $_AR_HOSTNAME/$PROJECT_ID/cloud-run-source-deploy/$REPO_NAME/$_SERVICE_NAME:latest
    id: Build
  - name: gcr.io/cloud-builders/docker
    args:
      - push
      - >-
        $_AR_HOSTNAME/$PROJECT_ID/cloud-run-source-deploy/$REPO_NAME/$_SERVICE_NAME:$COMMIT_SHA
    id: Push

  # - id: "apply migrations"
  #   name: "gcr.io/google-appengine/exec-wrapper"
  #   secretEnv:
  #     - DJANGO_SECRET
  #     - DATABASE_SELECTOR
  #     -
  #   args:
  #     [
  #       "-i",
  #       "$_AR_HOSTNAME/$PROJECT_ID/cloud-run-source-deploy/$REPO_NAME/$_SERVICE_NAME:$COMMIT_SHA",
  #       "-s",
  #       "jobescape-v2:us-east1:ai-academy-production",
  #       "-e",
  #       "DJANGO_SECRET",
  #       "-e",
  #       "DATABASE_SELECTOR",
  #       "-e",
  #       "DEBUG=true",
  #       "-e",
  #       "STAGE=true",
  #       "--",
  #       "python",
  #       "manage.py",
  #       "migrate",
  #     ]
  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk:slim"
    args:
      - run
      - services
      - update
      - $_SERVICE_NAME
      - "--platform=managed"
      - >-
        --image=$_AR_HOSTNAME/$PROJECT_ID/cloud-run-source-deploy/$REPO_NAME/$_SERVICE_NAME:$COMMIT_SHA
      - >-
        --labels=managed-by=gcp-cloud-build-deploy-cloud-run,commit-sha=$COMMIT_SHA,gcb-build-id=$BUILD_ID,gcb-trigger-id=$_TRIGGER_ID
      - "--region=$_DEPLOY_REGION"
      - "--quiet"
    id: Deploy
    entrypoint: gcloud

images:
  - $_AR_HOSTNAME/$PROJECT_ID/cloud-run-source-deploy/$REPO_NAME/$_SERVICE_NAME:$COMMIT_SHA
  - $_AR_HOSTNAME/$PROJECT_ID/cloud-run-source-deploy/$REPO_NAME/$_SERVICE_NAME:latest
options:
  substitutionOption: ALLOW_LOOSE
  logging: CLOUD_LOGGING_ONLY
substitutions:
  _TRIGGER_ID: cda416e5-b957-4f52-abe6-7492f136dd00
  _AR_HOSTNAME: us-east1-docker.pkg.dev
  _PLATFORM: managed
  _SERVICE_NAME: users-stage
  _DEPLOY_REGION: us-east1

tags:
  - gcp-cloud-build-deploy-cloud-run
  - gcp-cloud-build-deploy-cloud-run-managed
  - users-stage
# availableSecrets:
#   secretManager:
#     - versionName: projects/$PROJECT_ID/secrets/ACADEMY_DJANGO_SECRET/versions/latest
#       env: DJANGO_SECRET
#     - versionName: projects/$PROJECT_ID/secrets/PROD_DATABASE_SELECTOR/versions/latest
#       env: DATABASE_SELECTOR
#     - versionName: projects/$PROJECT_ID/secrets/PROD_GENERATE_TASKS/versions/latest
